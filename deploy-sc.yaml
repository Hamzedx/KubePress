apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: sc-wp

provisioner: efs.csi.aws.com

parameters:
  provisioningMode: efs-ap
  fileSystemId: fs-07e06b9c1460fa143
  directoryPerms: "777"

mountOptions:
  - tls

reclaimPolicy: Retain
volumeBindingMode: Immediate
